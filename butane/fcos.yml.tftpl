variant: fcos
version: 1.5.0

passwd:
  groups:
    - name: homelab
      gid: 1000
    - name: core
      gid: 1001
  users:
    - name: core
      uid: 1001
      primary_group: core
      ssh_authorized_keys:
%{ for _, ssh_key in ssh_keys ~}
        - ${ssh_key}
%{ endfor ~}
    - name: homelab
      # Stable identity for application data and VirtIO-FS ownership.
      uid: 1000
      primary_group: homelab
      home_dir: /var/home/homelab
      shell: /bin/bash
      groups:
        - systemd-journal
      ssh_authorized_keys:
%{ for _, ssh_key in homelab_ssh_keys ~}
        - ${ssh_key}
%{ endfor ~}

storage:
  directories:
    # Config dir
    - path: /var/home/homelab/.config
      user:
        name: homelab
      group:
        name: homelab

    # Systemd user dir
    - path: /var/home/homelab/.config/containers
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.config/systemd
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.config/systemd/user
      user:
        name: homelab
      group:
        name: homelab
    # Declared with the user, or Ignition creates them as root while linking units
    - path: /var/home/homelab/.config/systemd/user/default.target.wants
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.config/systemd/user/timers.target.wants
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.config/systemd/user/podman.service.d
      user:
        name: homelab
      group:
        name: homelab

    # Ignition creates implicit parents as root; declare the whole local tree.
    - path: /var/home/homelab/.local
      mode: 0755
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.local/bin
      mode: 0755
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.local/state
      mode: 0700
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.local/state/homelab
      mode: 0700
      user:
        name: homelab
      group:
        name: homelab
    # Backup snapshot mounting points
    - path: /var/mnt/snapshots/backblaze
    - path: /var/mnt/snapshots/storj

  links:
    # Enable Podman socket for Traefik
    - path: /var/home/homelab/.config/systemd/user/timers.target.wants/podman.socket
      target: /usr/lib/systemd/user/podman.socket
      user:
        name: homelab
      group:
        name: homelab

    # Enable Podman auto updates
    - path: /var/home/homelab/.config/systemd/user/timers.target.wants/podman-auto-update.timer
      target: /usr/lib/systemd/user/podman-auto-update.timer
      user:
        name: homelab
      group:
        name: homelab

    # Hometown timezone
    - path: /etc/localtime
      target: ../usr/share/zoneinfo/Europe/Belgrade

    # Enable systemd_exporter
    - path: /var/home/homelab/.config/systemd/user/default.target.wants/systemd-exporter.service
      target: /var/home/homelab/.config/systemd/user/systemd-exporter.service
      user:
        name: homelab
      group:
        name: homelab

    # Enable node_exporter
    - path: /var/home/homelab/.config/systemd/user/default.target.wants/node-exporter.service
      target: /var/home/homelab/.config/systemd/user/node-exporter.service
      user:
        name: homelab
      group:
        name: homelab

  files:
    # Fixed, non-overlapping rootless container ID ranges.
    - path: /etc/subuid
      overwrite: true
      mode: 0644
      contents:
        inline: |
          homelab:524288:65536
          core:655360:65536
    - path: /etc/subgid
      overwrite: true
      mode: 0644
      contents:
        inline: |
          homelab:524288:65536
          core:655360:65536

    # Podman storage for homelab.
    - path: /var/home/homelab/.config/containers/storage.conf
      user:
        name: homelab
      group:
        name: homelab
      contents:
        inline: |
          [storage]
          driver = "overlay"
          graphroot = "/mnt/docker/homelab"

    - path: /etc/containers/storage.conf
      contents:
        inline: |
          [storage]
          driver = "overlay"
          rootless_storage_path = "/var/mnt/docker/$USER"

    # Enable linger so containers can continue to run even after homelab user logouts
    - path: /var/lib/systemd/linger/homelab

    # Set machine hostname
    - path: /etc/hostname
      contents:
        inline: ${hostname}

    # Import Step CA root certificate
    - path: /etc/pki/ca-trust/source/anchors/step-online-ca.pem
      contents:
        inline: |
          ${indent(10, root_ca)}

    # Enable zram swap
    - path: /etc/systemd/zram-generator.conf
      contents:
        inline: |
          [zram0]

    - path: /etc/sysctl.d/90-traefik.conf
      contents:
        inline: |
          net.ipv4.ip_unprivileged_port_start=80

    - path: /etc/sysctl.d/90-bbr.conf
      contents:
        inline: |
          net.core.default_qdisc = cake
          net.ipv4.tcp_congestion_control = bbr

    - path: /etc/sysctl.d/90-inotify.conf
      contents:
        inline: |
          fs.inotify.max_user_instances = 1024

    - path: /etc/ssh/sshd_config.d/20-increase-max-auth-tries.conf
      mode: 0644
      contents:
        inline: |
          # Allow multiple identities offered by SSH agents.
          MaxAuthTries 10

    # Firewall configuration
    - path: /etc/sysconfig/nftables.conf
      overwrite: true
      mode: 0644
      contents:
        inline: |
          include "/etc/nftables/main.nft"

    - path: /etc/nftables/main.nft
      overwrite: true
      mode: 0644
      contents:
        inline: |
          ${indent(10, firewall_config)}

    # download systemd_exporter and create systemd user service
    - path: /var/home/homelab/.local/bin/systemd_exporter.tar.gz
      contents:
        source: https://github.com/prometheus-community/systemd_exporter/releases/download/v0.7.0/systemd_exporter-0.7.0.linux-amd64.tar.gz
        verification:
          hash: sha256-2d995ca20249aeeac8f507173176ce5d162f17470a98ca66e289c85b388480c3
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.config/systemd/user/systemd-exporter.service
      contents:
        inline: |
          [Unit]
          Description=Systemd Exporter

          [Service]
          Type=simple
          WorkingDirectory=/var/home/homelab/.local/bin
          ExecStartPre=/usr/bin/tar -xf systemd_exporter.tar.gz --strip-components=1 --wildcards '*/systemd_exporter'
          ExecStart=/var/home/homelab/.local/bin/systemd_exporter

          [Install]
          WantedBy=default.target
      user:
        name: homelab
      group:
        name: homelab

    # download node_exporter and create systemd user service
    - path: /var/home/homelab/.local/bin/node_exporter.tar.gz
      contents:
        source: https://github.com/prometheus/node_exporter/releases/download/v1.12.1/node_exporter-1.12.1.linux-amd64.tar.gz
        verification:
          hash: sha256-b51d8a76aa2a9156a55d501aca6276fae09e262259a5e4e831d2c2222f084e63
      user:
        name: homelab
      group:
        name: homelab
    - path: /var/home/homelab/.config/systemd/user/node-exporter.service
      contents:
        inline: |
          [Unit]
          Description=Node Exporter

          [Service]
          Type=simple
          WorkingDirectory=/var/home/homelab/.local/bin
          ExecStartPre=/usr/bin/tar -xf node_exporter.tar.gz --strip-components=1 --wildcards '*/node_exporter'
          ExecStart=/var/home/homelab/.local/bin/node_exporter --collector.systemd --collector.processes

          [Install]
          WantedBy=default.target

    # Disable Podman access logs
    - path: /var/home/homelab/.config/systemd/user/podman.service.d/override.conf
      contents:
        inline: |
          [Service]
          Environment=LOGGING="--log-level=warn"

    # Root restic processes obtain current secrets from homelab's Podman store.
    - path: /etc/restic/run
      mode: 0755
      contents:
        inline: |
          ${indent(10, restic_runner)}

    # restic excludes; the wildcard covers both snapshot mounts
    - path: /etc/restic/excludes.txt
      overwrite: true
      mode: 0644
      contents:
        inline: |
          # Caches and indexes that the application rebuilds on its own
          /mnt/snapshots/*/app_data/alloy
          /mnt/snapshots/*/app_data/crowdsec/data
          /mnt/snapshots/*/app_data/crowdsec/staging
          /mnt/snapshots/*/app_data/grafana/data/plugins
          /mnt/snapshots/*/app_data/hister/llamacpp
          /mnt/snapshots/*/app_data/karakeep/meilisearch
          /mnt/snapshots/*/app_data/open-webui/cache
          /mnt/snapshots/*/app_data/traefik/plugins

          # Media Synapse re-fetches from the originating homeserver
          /mnt/snapshots/*/app_data/matrix/synapse/media_store/remote_content
          /mnt/snapshots/*/app_data/matrix/synapse/media_store/remote_thumbnail
          /mnt/snapshots/*/app_data/matrix/synapse/media_store/local_thumbnails

          # Plex rebuilds everything here from the library on disk
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Cache
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Codecs
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Crash Reports
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Drivers
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Logs
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Media
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Metadata
          /mnt/snapshots/*/app_data/plex/Library/Application Support/Plex Media Server/Scanners

          # Volatile stores: sessions and queues, worthless once restored
          /mnt/snapshots/*/app_data/*/valkey
          /mnt/snapshots/*/app_data/immich/redis
          /mnt/snapshots/*/app_data/rmqtt/logs
          /mnt/snapshots/*/app_data/*/*.log

systemd:
  units:
    - name: install-additional-software.service
      enabled: true
      contents: |
        [Unit]
        Description=Additional software installer
        Wants=network-online.target
        After=network-online.target
        Before=zincati.service
        ConditionPathExists=!/var/lib/%N.stamp

        [Service]
        Type=oneshot
        RemainAfterExit=yes
        ExecStart=/usr/bin/rpm-ostree install --allow-inactive --assumeyes --reboot qemu-guest-agent restic btop nvtop
        ExecStart=/bin/touch /var/lib/%N.stamp

        [Install]
        WantedBy=multi-user.target

    - name: attach-local-disk.service
      enabled: true
      contents: |
        [Unit]
        Description=Activate existing homelab data disk via VirtIO SCSI
        DefaultDependencies=no
        After=systemd-udev-trigger.service
        Before=var-mnt-docker.mount local-fs.target shutdown.target
        Conflicts=shutdown.target

        [Service]
        Type=oneshot
        RemainAfterExit=yes
        ExecStart=/usr/bin/udevadm wait --timeout=60 /dev/disk/by-id/lvm-pv-uuid-${data_pv_uuid}
        ExecStart=/usr/sbin/lvmdevices --adddev /dev/disk/by-id/lvm-pv-uuid-${data_pv_uuid}
        ExecStart=/usr/sbin/vgchange -ay vg0

        [Install]
        WantedBy=local-fs.target

    - name: var-mnt-docker.mount
      enabled: true
      contents: |
        [Unit]
        Description=Mount existing homelab data via VirtIO SCSI
        Requires=attach-local-disk.service
        After=attach-local-disk.service
        Before=local-fs.target

        [Mount]
        What=/dev/vg0/lv0
        Where=/var/mnt/docker
        Type=xfs
        Options=defaults

        [Install]
        WantedBy=local-fs.target

    - name: user@1000.service
      dropins:
        - name: 90-storage.conf
          contents: |
            [Unit]
            RequiresMountsFor=/var/mnt/docker /var/mnt/personal /var/mnt/media /var/mnt/observability /var/mnt/random

            [Service]
            ExecStartPre=/usr/bin/mountpoint -q /var/mnt/docker
            ExecStartPre=/usr/bin/mountpoint -q /var/mnt/personal
            ExecStartPre=/usr/bin/mountpoint -q /var/mnt/media
            ExecStartPre=/usr/bin/mountpoint -q /var/mnt/observability
            ExecStartPre=/usr/bin/mountpoint -q /var/mnt/random

    - name: podman-fix-selinux-context.service
      enabled: true
      contents: |
        [Unit]
        Description=Fix SELinux context for Podman storage
        Requires=var-mnt-docker.mount
        After=var-mnt-docker.mount
        ConditionPathExists=!/var/lib/%N.stamp

        [Service]
        Type=oneshot
        RemainAfterExit=yes
        ExecStart=/usr/bin/chcon -R -t container_file_t /var/mnt/docker/homelab
        ExecStart=/bin/touch /var/lib/%N.stamp

        [Install]
        WantedBy=multi-user.target

    - name: var-mnt-random.mount
      enabled: true
      contents: |
        [Unit]
        Description=Mount random dataset from PVE via VirtIO-FS
        Before=local-fs.target

        [Mount]
        What=homelab-random
        Where=/var/mnt/random
        Type=virtiofs
        Options=relatime,context=system_u:object_r:container_file_t:s0

        [Install]
        WantedBy=local-fs.target

    - name: var-mnt-observability.mount
      enabled: true
      contents: |
        [Unit]
        Description=Mount observability dataset from PVE via VirtIO-FS
        Before=local-fs.target

        [Mount]
        What=homelab-observability
        Where=/var/mnt/observability
        Type=virtiofs
        Options=relatime,context=system_u:object_r:container_file_t:s0

        [Install]
        WantedBy=local-fs.target

    - name: var-mnt-media.mount
      enabled: true
      contents: |
        [Unit]
        Description=Mount media dataset from PVE via VirtIO-FS
        Before=local-fs.target

        [Mount]
        What=homelab-media
        Where=/var/mnt/media
        Type=virtiofs
        Options=relatime,context=system_u:object_r:container_file_t:s0

        [Install]
        WantedBy=local-fs.target

    - name: var-mnt-personal.mount
      enabled: true
      contents: |
        [Unit]
        Description=Mount personal dataset from PVE via VirtIO-FS
        Before=local-fs.target

        [Mount]
        What=homelab-personal
        Where=/var/mnt/personal
        Type=virtiofs
        Options=relatime,context=system_u:object_r:container_file_t:s0

        [Install]
        WantedBy=local-fs.target

    - name: nftables.service
      enabled: true

    - name: blog-deploy-target.service
      enabled: true
      contents: |
        [Unit]
        Description=Prepare the blog deploy target
        RequiresMountsFor=/var/mnt/docker
        Before=multi-user.target

        [Service]
        Type=oneshot
        RemainAfterExit=yes
        ExecStart=/usr/bin/mkdir -p /var/mnt/docker/blog
        ExecStart=/usr/bin/chown -R homelab:homelab /var/mnt/docker/blog
        ExecStart=/usr/bin/chmod 0755 /var/mnt/docker/blog
        # New files inherit the context, so rsync output stays readable to the container
        ExecStart=/usr/bin/chcon -R -t container_file_t /var/mnt/docker/blog

        [Install]
        WantedBy=multi-user.target

    - name: restic-backblaze.service
      contents: |
        [Unit]
        Description=Backup Docker data to Backblaze
        Wants=network-online.target
        After=network-online.target
        RequiresMountsFor=/var/mnt/docker

        [Service]
        Type=oneshot
        Environment=RESTIC_CACHE_DIR=/var/mnt/docker/restic-cache
        ExecStartPre=/usr/bin/mountpoint -q /var/mnt/docker
        # Fail on missing secrets before creating a snapshot.
        ExecStartPre=/etc/restic/run backblaze version
        # Create LVM snapshot and do backup
        ExecStart=/bin/bash -c "lvremove -y vg0/restic-backblaze || true"
        ExecStart=/usr/sbin/lvcreate --size 15G --snapshot --name restic-backblaze vg0/lv0
        ExecStart=/usr/bin/mount -o nouuid /dev/vg0/restic-backblaze /mnt/snapshots/backblaze
        ExecStart=/etc/restic/run backblaze -r b2:krasovsky-homelab:app-data --verbose --exclude-file=/etc/restic/excludes.txt backup /mnt/snapshots/backblaze/app_data
        ExecStart=/bin/bash -c "umount /mnt/snapshots/backblaze || true"
        ExecStart=/usr/sbin/lvremove -y vg0/restic-backblaze

        [Install]
        WantedBy=multi-user.target

    - name: restic-backblaze.timer
      enabled: true
      contents: |
        [Unit]
        Description=Daily backup to Backblaze

        [Timer]
        OnCalendar=daily
        RandomizedDelaySec=3600
        Unit=restic-backblaze.service

        [Install]
        WantedBy=timers.target

    - name: restic-storj.service
      contents: |
        [Unit]
        Description=Backup Docker data to Storj
        Wants=network-online.target
        After=network-online.target
        RequiresMountsFor=/var/mnt/docker

        [Service]
        Type=oneshot
        Environment=RESTIC_CACHE_DIR=/var/mnt/docker/restic-cache
        ExecStartPre=/usr/bin/mountpoint -q /var/mnt/docker
        # Fail on missing secrets before creating a snapshot.
        ExecStartPre=/etc/restic/run storj version
        # Create LVM snapshot and do backup
        ExecStart=/bin/bash -c "lvremove -y vg0/restic-storj || true"
        ExecStart=/usr/sbin/lvcreate --size 15G --snapshot --name restic-storj vg0/lv0
        ExecStart=/usr/bin/mount -o nouuid /dev/vg0/restic-storj /mnt/snapshots/storj
        ExecStart=/etc/restic/run storj -r s3:https://gateway.eu1.storjshare.io/homelab-backup/app-data --verbose --exclude-file=/etc/restic/excludes.txt backup /mnt/snapshots/storj/app_data
        ExecStart=/bin/bash -c "umount /mnt/snapshots/storj || true"
        ExecStart=/usr/sbin/lvremove -y vg0/restic-storj

        [Install]
        WantedBy=multi-user.target

    - name: restic-storj.timer
      enabled: true
      contents: |
        [Unit]
        Description=Daily backup to Storj

        [Timer]
        OnCalendar=daily
        RandomizedDelaySec=3600
        Unit=restic-storj.service

        [Install]
        WantedBy=timers.target

    - name: restic-backblaze-prune.service
      contents: |
        [Unit]
        Description=Apply retention policy to the Backblaze repository
        Wants=network-online.target
        After=network-online.target restic-backblaze.service
        RequiresMountsFor=/var/mnt/docker

        [Service]
        Type=oneshot
        Environment=RESTIC_CACHE_DIR=/var/mnt/docker/restic-cache
        ExecStart=/etc/restic/run backblaze -r b2:krasovsky-homelab:app-data --verbose forget --keep-daily 14 --keep-weekly 8 --keep-monthly 12 --prune

        [Install]
        WantedBy=multi-user.target

    - name: restic-backblaze-prune.timer
      enabled: true
      contents: |
        [Unit]
        Description=Weekly retention pass on Backblaze

        [Timer]
        OnCalendar=Sun *-*-* 03:00:00
        RandomizedDelaySec=900
        Unit=restic-backblaze-prune.service

        [Install]
        WantedBy=timers.target

    - name: restic-storj-prune.service
      contents: |
        [Unit]
        Description=Apply retention policy to the Storj repository
        Wants=network-online.target
        After=network-online.target restic-storj.service
        RequiresMountsFor=/var/mnt/docker

        [Service]
        Type=oneshot
        Environment=RESTIC_CACHE_DIR=/var/mnt/docker/restic-cache
        ExecStart=/etc/restic/run storj -r s3:https://gateway.eu1.storjshare.io/homelab-backup/app-data --verbose forget --keep-daily 14 --keep-weekly 8 --keep-monthly 12 --prune

        [Install]
        WantedBy=multi-user.target

    - name: restic-storj-prune.timer
      enabled: true
      contents: |
        [Unit]
        Description=Weekly retention pass on Storj

        [Timer]
        OnCalendar=Sun *-*-* 05:00:00
        RandomizedDelaySec=900
        Unit=restic-storj-prune.service

        [Install]
        WantedBy=timers.target

kernel_arguments:
  should_exist:
    - pcie_aspm.policy=powersupersave ifname=infra:${fcos_mac_address} ip=${fcos_ip}::${gateway_ip}:${subnet_mask}:${hostname}:infra:none:${dns_ip}
