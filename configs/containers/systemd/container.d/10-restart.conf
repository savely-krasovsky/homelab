[Container]
# Podman SIGKILLs at 10s by default, too short for databases
StopTimeout=60

[Service]
# Space out automatic restarts after transient failures; keep systemd's start limits.
RestartSec=10s
