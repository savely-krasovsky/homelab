[Container]
# Podman SIGKILLs at 10s by default; the unit then ends up failed and takes the whole
# teardown down with it. Long enough for databases to close cleanly.
StopTimeout=60

[Service]
# Space out automatic restarts after transient failures; keep systemd's start limits.
RestartSec=10s
